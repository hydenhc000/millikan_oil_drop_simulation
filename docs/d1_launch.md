# 1_launch.md

    1) directory:
    
        - cd desktop/dev/millikan_oil_drop_simulations

    2) venv (IOS & linux):

        - create:

            - to create = cmd: python3 -m venv venv
            - to activate = cmd: source venv/bin/activate
            - to deactivate = key: (ctrl C)

    3) dependencies (IOS & linux):
        
        - linux & IOS:
            
            - to create = cmd: pip freeze > requirements.txt
            - to update = cmd: pip freeze > requirements.txt
            - to install = cmd: pip install -r requirements.txt      

    4) launch:

        - python3 run.py